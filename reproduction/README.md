# ADAPT execution support

See the [repository README](../README.md) for prerequisites, exact run commands, provenance, and limitations.

From the repository root, after installing Ubuntu prerequisites:

```bash
python3 reproduction/setup_and_run.py
```

Use this entry point, not the upstream cleanup scripts. New run outputs go to `runs/smoke-*`.
